import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MessageriefrreePage } from './messageriefrree.page';

describe('MessageriefrreePage', () => {
  let component: MessageriefrreePage;
  let fixture: ComponentFixture<MessageriefrreePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MessageriefrreePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
