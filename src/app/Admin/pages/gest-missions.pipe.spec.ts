import { GestMissionsPipe } from './gest-missions.pipe';

describe('GestMissionsPipe', () => {
  it('create an instance', () => {
    const pipe = new GestMissionsPipe();
    expect(pipe).toBeTruthy();
  });
});
